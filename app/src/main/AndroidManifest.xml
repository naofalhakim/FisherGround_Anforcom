<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.illiyinmagang.miafandi.donaku">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Slider.SliderActivity" />
        <activity
            android:name=".LoginActivity"
            android:windowSoftInputMode="adjustPan|adjustResize|adjustUnspecified" />
        <activity android:name=".HomeActivity" />
        <activity
            android:name=".introLoginActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".HomeUserActivity" />
        <!-- <<<<<<< HEAD -->

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".LogTransaksiActivity" />
        <!-- ======= -->
        <activity android:name=".Fragment.HomeUser.DetailJual" />
        <activity android:name=".Fragment.Investasi.DetailInvestasi" />
        <!-- &gt;>>>>>> abc2120c8bde36e116727cb3702d053f986b2365 -->
        <activity android:name=".DetailPenjualanActivity"></activity>
    </application>

</manifest>